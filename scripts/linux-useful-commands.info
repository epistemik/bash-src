
# Copyright (c) 2026 Mark Sattolo <epistemik@gmail.com>
# modified 2026-04-23


  command line loops
----------------------
 for i in {16..35}; do popd +16; done

 for x in $(ls); do file $x; done



  FIND files by filename extension
------------------------------------
 > find -maxdepth 9 -type f -name "*.csv" -print

  FIND files by date
----------------------
 1) Move all files older than 10 days from current folder to subfolder 'OLD'
   > find . -maxdepth 1 -type f -mtime +10 -exec mv {} OLD/ \;

  FIND files by size
----------------------
 1) List all files smaller than 10 bytes in current folder
   > find . -maxdepth 1 -type f -size -10c -print

 2) DELETE all files SMALLER than 98 bytes in current folder
   > find . -maxdepth 1 -type f -size -98c -delete

 3) DELETE all files LARGER than 2 Mbytes in current folder
   > find . -maxdepth 1 -type f -size +2M -delete

  Find and DELETE Files Older Than x Days
-------------------------------------------
 1) Delete files older than 90 days in <directory>
   > find <directory> -type f -mtime +90 -delete

 2) Delete, INCLUDING sub-folders, all '.log' files older than 30 days
   > find -maxdepth 9 -name "*.log" -type f -mtime +30 -delete



  pause a running command
---------------------------
Ctrl + Z: Suspends the foreground process. It stops the task and returns you to the prompt.
Resume: Type 'fg' to bring it back to the foreground or 'bg' to let it run in the background.
Ctrl + S: Pauses the output of the terminal. The process may keep running in the background until its output buffer is full.
Resume Output: Type Ctrl + Q.



  get release info
--------------------
1. cat /etc/issue
2. hostnamectl
3. lsb_release -a
4. cat /etc/linuxmint/info
5. cat /etc/os-release
6. cat /etc/upstream-release/lsb-release



  get info for a package
--------------------------------------------------
 apt show <package_name>

  install some/all the dependencies for a package
--------------------------------------------------
 apt build-dep <package_name>



  list the packages matching a pattern
---------------------------------------
 dpkg -l |grep <pattern>

  get the location of a package's files
-----------------------------------------
 dpkg -L <package_name> |more



  list current modules
------------------------
 lsmod |more



  add new or updated lib folders to system
--------------------------------------------
 sudo /sbin/ldconfig -v



  in lib folder, find object reference
----------------------------------------
 nm lib*.so* |grep object

